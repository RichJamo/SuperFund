{
  "version": 2,
  "projects": {
    "frontend": {
      "src": "frontend/",
      "use": "@vercel/static-build",
      "config": {
        "outputDirectory": "build"
      }
    },
    "server": {
      "src": "server/",
      "use": "@vercel/node",
      "config": {
        "distDir": "dist"
      }
    }
  }
}
